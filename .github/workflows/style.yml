name: Format python code

on:
  push:
    branches:
      - 'main'
    paths:
      - '**.py'
  pull_request:
    branches:
      - 'main'

jobs:
  formatting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: formatting
        uses: psf/black@stable
        with:
          options: "--verbose"
          src: "./bangumi"

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v3
        with:
          commit-message: "style: auto fixes"
          title: Auto code style fixes
          body: This is an auto-generated PR with style fixes.
          labels: black, automated pr
          branch: code-style-patches